BANXICO_TOKEN=

# Email (Phase 3). Gmail: enable 2-step verification, then create an app
# password at https://myaccount.google.com/apppasswords and use it as
# SMTP_PASSWORD. Any other SMTP service works by overriding SMTP_HOST/PORT.
SMTP_USER=
SMTP_PASSWORD=
EMAIL_TO=
# Optional (defaults shown / EMAIL_FROM defaults to SMTP_USER):
#SMTP_HOST=smtp.gmail.com
#SMTP_PORT=587
#EMAIL_FROM=
